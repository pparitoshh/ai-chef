# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Env
.env

# Data (downloaded via pipeline); keep the final sample + its precomputed
# embeddings (in-memory search backend) in git
# NOTE: `data/*` (not `data/`) so the negation below can re-include the files
data/*
!data/recipes_10k.csv
!data/recipes_10k_embeddings.npy

# ONNX models downloaded at runtime by the in-memory search backend
models/

# uv / build
dist/
*.egg-info/

# OS / editors
.DS_Store
.idea/
.vscode/

# Streamlit local secrets
.streamlit/secrets.toml
